There are some personal tools
Most of them are .py files.
Please make sure you have install the related libraries
Such as :
python -m pip install numpy 
python -m pip install matplotlib    
python -m pip install pandas
